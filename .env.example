# OpenAI API (optional)
OPENAI_API_KEY=your_openai_api_key_here

# Hugging Face (for accessing gated models)
HUGGINGFACE_TOKEN=your_huggingface_token_here

# Vector DB settings
VECTOR_DB_PATH=./data/vectordb

# Model settings
# Options: "local" or "api"
LLM_MODE=local
# If using local model, specify the model path or name
LOCAL_MODEL_NAME=mistral-7b-instruct-v0.2
# If using API model, specify the model name
API_MODEL_NAME=gpt-3.5-turbo

# Embedding settings
# Options: "local" or "api"
EMBEDDING_MODE=local
# If using local embeddings, specify the model name
LOCAL_EMBEDDING_MODEL=all-MiniLM-L6-v2
# If using API embeddings, specify the model name
API_EMBEDDING_MODEL=text-embedding-ada-002

# Server settings
HOST=0.0.0.0
PORT=8000